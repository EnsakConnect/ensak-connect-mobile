package com.ensak.connect.presentation.home;

import com.ensak.connect.repository.profile.model.ProfileResponseDTO;

public interface OnClickListener {
    void onClick(ProfileResponseDTO profile);
}
